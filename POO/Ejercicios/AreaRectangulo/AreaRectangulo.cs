/* public class AreaRectangulo
{
    public double baseRect { get; set; }
    public double alturaRect { get; set; }

    public void Area()
    {
        double resultadoArea = Calcular(baseRect, alturaRect);
        Console.WriteLine(resultadoArea);
    }
    private double Calcular(double bases, double altura)
    {
        if (bases > 0 && altura > 0)
        {
            double area = bases * altura;
            return area;
        }
        else
        {
            return 0;
        }
    }
} */